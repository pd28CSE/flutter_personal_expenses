class Transaction {
  String id;
  String title;
  double price;
  DateTime dateTime;

  Transaction({
    required this.id,
    required this.title,
    required this.price,
    required this.dateTime,
  });
}
